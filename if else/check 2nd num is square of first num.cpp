//write a program that inputs two number and find if second number is square of first
#include<iostream>
using namespace std;
int main()
{
	int a,b;
	cout<<"Enter a number : ";
	cin>>a;
	cout<<"Enter a number : ";
	cin>>b;
	if(a*a==b)
	cout<<"2nd number is square of  first number : ";
	else
	cout<<"2nd number is not square of first number : ";
	}
