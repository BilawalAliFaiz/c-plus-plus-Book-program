//write a program that input three number and display wether all number are equall or not by using nested if condition.
#include<iostream>
using namespace std;
int main()
{
	int a,b,c;
	cout<<"Enter three number ";
	cin>>a>>b>>c;
	if(a==b)
	if(a==c)
	cout<<"All number are equall :";
	else
	cout<<"Number are different :";
	else
	cout<<"Number are different :";
}

