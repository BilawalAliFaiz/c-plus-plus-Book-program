//write a program that input number and determine wheather it is positive ,nagtive or zero
#include<iostream>
using namespace std;
int main()
{
	int number;
	cout<<"Enter a number : ";
	cin>>number;
	if(number>0)
	
		cout<<" Number is positive : ";
		
	
	if (number<0)
	
		cout<<"Number is Nagative : ";
	if (number==0)
		cout<<"Number is equall to zero :";
}
